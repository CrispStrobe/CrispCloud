name: crisp
description: CrispCloud CLI companion — manage cloud storage from the terminal.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.4.2
  yaml: ^3.1.0
  http: ^1.2.0
  crypto: ^3.0.3
  path: ^1.9.1
  dartssh2: ^2.10.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.0
