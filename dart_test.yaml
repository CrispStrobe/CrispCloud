tags:
  benchmark:
    skip: "Benchmarks are load-sensitive. Run with: flutter test --tags benchmark --run-skipped"
  live:
    skip: "Live tests require provider credentials. Run with: flutter test --tags live"
  integration:
    skip: "Integration tests use mock servers with I/O. Run with: flutter test --tags integration"
  golden:
    skip: "Golden tests require baseline images. Run with: flutter test --tags golden"
  connection_dialog:
    skip: "Connection dialog widget tests. Run with: flutter test --tags connection_dialog"
  deploy:
    skip: "Live deployment verification. Run with: flutter test --tags deploy"
